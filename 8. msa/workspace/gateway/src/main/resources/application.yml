<<<<<<< HEAD
server:
  port: 8000
spring:
  profiles:    active: default
  application:
    name: gateway
  cloud:
    gateway:
      # http(s)://gateway주소/서비스명
      # -> http(s)://서비스명
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
          
    # 서비스별 경로 개별 등록
#      routes:
#      - id: service1
#        uri: lb://SERVICE-1
#        predicates:
#        - Path=/service-1/**
#        filters:
#        - RewritePath=/service-1/(?<path>.*),/$\{path}
        
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
management:
  endpoints:
    web:
      exposure:
        include: "*"
        
        
---
spring:  profiles: dev
eureka:  client:    service-url:
      defaultZone: http://ec2-3-34-216-213.ap-northeast-2.compute.amazonaws.com:8761/eureka/    instance:
      hostname: ec2-3-34-216-213.ap-northeast-2.compute.amazonaws.com    
=======
server:
  port: 8000
spring:
  application:
    name: gateway
  cloud:
    gateway:
      # http(s)://gateway주소/서비스명
      # -> http(s)://서비스명
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
          
    # 서비스별 경로 개별 등록
#      routes:
#      - id: service1
#        uri: lb://SERVICE-1
#        predicates:
#        - Path=/service-1/**
#        filters:
#        - RewritePath=/service-1/(?<path>.*),/$\{path}
        
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
management:
  endpoints:
    web:
      exposure:
        include: "*"
>>>>>>> 3b5904d61b63e66ddf9d8cb1a8cb00680bdfa2f0
