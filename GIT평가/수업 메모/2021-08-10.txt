2021-08-10

문자열


1. 특수문자 

"", '' 안에 어퍼스트로피( ' ) 쓰려면 백슬래쉬 ( \ ' ) 와 함께 써야한다. 그냥 쓰면 따옴표 닫힘으로 인식하기 때문.
반면, `` 백틱안에 쓰면 인식을 한다. 백틱안에서는 \' , ' 모두가능

역슬래쉬는 두개 입력해야 한개로 출력됨. \\ => \

length 프로퍼티: str.length O / str.length( ) X
함수가 아니고 숫자가 저장되는 프로퍼티임. 뒤에 괄호 붙일 필요 없음

.indexOf("찾을 글자" , 숫자{몇번째부터?}) 
찾으면 글자가 있는 순서를 보여줌, 못찾으면 -1 출력
몇번째부터 찾을지 입력하면 그곳부터 탐색함.


